# 留空时前端走同源 /api，开发环境由 Vite 代理转发到后端
# （目标由 TIANMING_BACKEND_ORIGIN 指定，默认 http://127.0.0.1:5002，即 old/ 冻结后端）。
# 仅当 API 部署在其他源时才填绝对地址。
VITE_API_BASE_URL=

# `npm run dev` 的代理目标；后端端口固定 5002，一般无需修改。
# TIANMING_BACKEND_ORIGIN=http://127.0.0.1:5002

# 可选监听地址；用 0.0.0.0 允许局域网访问。
# TIANMING_LISTEN_HOST=0.0.0.0
